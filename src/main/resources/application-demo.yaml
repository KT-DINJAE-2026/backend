spring:
  datasource:
    url: jdbc:h2:mem:backend-demo;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  docker:
    compose:
      enabled: false

app:
  demo:
    enabled: true
  topis:
    enabled: false
  holiday:
    enabled: false
  weather:
    enabled: false
  # demo는 고정 응답으로 FE 계약만 검증하므로 모델을 적재하지 않는다.
  model:
    enabled: false

logging:
  level:
    com.example.backend: INFO
